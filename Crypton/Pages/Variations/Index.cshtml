@page
@model Crypton.Pages.Variations.IndexModel

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Variation[0].variationType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Variation) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.variationType)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.variationID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.variationID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.variationID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
